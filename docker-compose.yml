services:
  mission-control:
    build: .
    ports:
      - '3000:3000'
    environment:
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL to a PostgreSQL connection string}
    restart: unless-stopped
